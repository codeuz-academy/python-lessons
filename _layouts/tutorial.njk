<!DOCTYPE html>
{% set tutorialItems = collections.tutorials %}
<html lang="{{ lang or 'en' }}">
  {% set isTutorial = true %}
  {% set useBrandIconFont = true %}
  {% include "head.njk" %}
  <body class="overflow-x-hidden">
    {% include "navbar.njk" %}
    <!-- Mobile Sidebar Drawer -->
    <div
      id="tutorial-sidebar-overlay"
      class="fixed inset-0 bg-black/50 z-[70] hidden transition-opacity duration-300 opacity-0 md:hidden"></div>
    <div
      id="tutorial-sidebar-drawer"
      class="fixed top-0 left-0 h-full w-4/5 max-w-[280px] bg-white z-[80] transform -translate-x-full transition-transform
        duration-300 md:hidden overflow-y-auto custom-scrollbar shadow-2xl">
      <div class="p-4 md:p-6">
        <div class="flex justify-between items-center mb-6">
          <h2 class="text-xl font-bold text-primary-500">Tutorial Material</h2>
          <button id="close-tutorial-sidebar" class="text-slate-400 hover:text-slate-600 focus:outline-none">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewbox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
          </button>
        </div>
        <div class="mobile-sidebar-content">
          {% include "sidebar-list.njk" %}
        </div>
      </div>
    </div>
    <div class="max-w-4xl mx-auto py-10 flex">
      {% include "sidebar.njk" %}
      <main class="w-full min-w-0 pb-2 px-4 md:w-9/12 tutorial">
        <div class="flex flex-col md:flex-row justify-between items-center mb-5 gap-4">
          <h1 class="text-primary-500 font-medium text-3xl flex-1">{{ title }}</h1>
          <div class="flex flex-col items-start justify-center md:items-end shrink-0 gap-[10px]">
            <span class="text-xs text-slate-500 pl-[30px] font-medium uppercase tracking-wide text-center">Share to</span>
            <div class="flex items-center gap-2.5">
              <a
                href="https://threads.net/intent/post?text={{ title | urlencode }}%20{{ site.url }}{{ page.url }}"
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex h-6 w-6 items-center justify-center no-underline hover:no-underline hover:opacity-80 transition-opacity"
                title="Share on Threads">
                <i class="fa-brands fa-threads text-[20px] leading-none text-black" aria-hidden="true"></i>
              </a>
              <a
                href="https://wa.me/?text={{ title | urlencode }}%20{{ site.url }}{{ page.url }}"
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex h-6 w-6 items-center justify-center no-underline hover:no-underline hover:opacity-80 transition-opacity"
                title="Share on WhatsApp">
                <i class="fa-brands fa-whatsapp text-[20px] leading-none text-[#25D366]" aria-hidden="true"></i>
              </a>
              <a
                href="https://twitter.com/intent/tweet?text={{ title | urlencode }}&url={{ site.url }}{{ page.url }}"
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex h-6 w-6 items-center justify-center no-underline hover:no-underline hover:opacity-80 transition-opacity"
                title="Share on X">
                <i class="fa-brands fa-x-twitter text-[20px] leading-none text-black" aria-hidden="true"></i>
              </a>
              <a
                href="https://www.linkedin.com/sharing/share-offsite/?url={{ site.url }}{{ page.url }}"
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex h-6 w-6 items-center justify-center no-underline hover:no-underline hover:opacity-80 transition-opacity"
                title="Share on LinkedIn">
                <i class="fa-brands fa-linkedin-in text-[20px] leading-none text-[#0A66C2]" aria-hidden="true"></i>
              </a>
            </div>
          </div>
        </div>
        {{ content | safe }}
        {% set tutorialNav = tutorialItems | getTutorialNav(page.url) %}
        <div class="mt-8">
          <p class="mb-6 text-sm text-slate-500">
            <a
              href="https://github.com/belajarpythoncom/belajarpython.com/blob/main/{{ (sourceInputPath or page.inputPath) | replace('./', '') }}"
              class="hover:text-primary-500">
              Edit this tutorial
            </a>
          </p>
          <div class="flex justify-between gap-x-2 md:gap-x-4">
            <div class="flex justify-start">
              {% if tutorialNav.prev %}
                <a href="{{ tutorialNav.prev.url }}" class="text-primary-300 hover:text-primary-300 flex h-10 md:h-12 items-center rounded-full bg-blue-500 bg-opacity-20 px-4 md:px-8 text-sm md:text-base hover:no-underline">
                  <svg xmlns="http://www.w3.org/2000/svg" class="mr-1 h-4 w-4 md:h-5 md:w-5" viewbox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm.707-10.293a1 1 0 00-1.414-1.414l-3 3a1 1 0 000 1.414l3 3a1 1 0 001.414-1.414L9.414 11H13a1 1 0 100-2H9.414l1.293-1.293z" clip-rule="evenodd" />
                  </svg>
                  <span class="-mt-0.5">{{ tutorialNav.prev.data.title }}</span>
                </a>
              {% endif %}
            </div>
            <div class="flex justify-end">
              {% if tutorialNav.next %}
                <a href="{{ tutorialNav.next.url }}" class="flex h-10 md:h-12 items-center rounded-full bg-gradient-to-l from-yellow-500 to-yellow-400 px-4 md:px-8 text-sm md:text-base text-black shadow-xl hover:text-black hover:no-underline hover:shadow">
                  <span class="-mt-0.5">{{ tutorialNav.next.data.title }}</span>
                  <svg xmlns="http://www.w3.org/2000/svg" class="ml-1 h-4 w-4 md:h-5 md:w-5" viewbox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.707l-3-3a1 1 0 00-1.414 1.414L10.586 9H7a1 1 0 100 2h3.586l-1.293 1.293a1 1 0 101.414 1.414l3-3a1 1 0 000-1.414z" clip-rule="evenodd" />
                  </svg>
                </a>
              {% else %}
                <a href="{{ tutorialNav.first.url }}" class="flex h-10 md:h-12 items-center rounded-full bg-gradient-to-l from-yellow-500 to-yellow-400 px-4 md:px-8 text-sm md:text-base text-black shadow-xl hover:text-black hover:no-underline hover:shadow">
                  <span class="-mt-0.5">Back to Start</span>
                  <svg xmlns="http://www.w3.org/2000/svg" class="ml-1 h-4 w-4 md:h-5 md:w-5" viewbox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.707l-3-3a1 1 0 00-1.414 1.414L10.586 9H7a1 1 0 100 2h3.586l-1.293 1.293a1 1 0 101.414 1.414l3-3a1 1 0 000-1.414z" clip-rule="evenodd" />
                  </svg>
                </a>
              {% endif %}
            </div>
          </div>
        </div>
      </main>
    </div>
    {% include "analytics.njk" %}
    <script>
      document.addEventListener('DOMContentLoaded', function () {
        function findScrollableParent(el) {
          let node = el ? el.parentElement : null;
          while (node && node !== document.body) {
            const style = window.getComputedStyle(node);
            const overflowY = style.overflowY;
            const isScrollable = (overflowY === 'auto' || overflowY === 'scroll') && node.scrollHeight > node.clientHeight;
            if (isScrollable) {
              return node;
            }
            node = node.parentElement;
          }
          return null;
        }

        function scrollActiveSidebarItemIntoView() {
          const activeLinks = document.querySelectorAll('[data-sidebar-link="true"][data-active="true"]');
          activeLinks.forEach((link) => {
            const scrollParent = findScrollableParent(link);
            if (scrollParent) {
              const parentRect = scrollParent.getBoundingClientRect();
              const linkRect = link.getBoundingClientRect();
              const offsetTop = linkRect.top - parentRect.top + scrollParent.scrollTop;
              scrollParent.scrollTop = Math.max(0, offsetTop - scrollParent.clientHeight / 2 + linkRect.height / 2);
            }
          });
        }

        scrollActiveSidebarItemIntoView();

        const toggleBtn = document.getElementById('mobile-tutorial-sidebar-toggle');
        const closeBtn = document.getElementById('close-tutorial-sidebar');
        const overlay = document.getElementById('tutorial-sidebar-overlay');
        const drawer = document.getElementById('tutorial-sidebar-drawer');
        const body = document.body;
        function toggleSidebar() {
          const isOpen = ! drawer.classList.contains('-translate-x-full');
          if (! isOpen) { // Open
            overlay.classList.remove('hidden');
            setTimeout(() => {
              overlay.classList.add('opacity-100');
              drawer.classList.remove('-translate-x-full');
            }, 10);
            body.classList.add('overflow-hidden');
          } else { // Close
            overlay.classList.remove('opacity-100');
            drawer.classList.add('-translate-x-full');
            setTimeout(() => {
              overlay.classList.add('hidden');
            }, 300);
            body.classList.remove('overflow-hidden');
          }
        }
        if (toggleBtn) 
          toggleBtn.addEventListener('click', toggleSidebar);
        
        if (closeBtn) 
          closeBtn.addEventListener('click', toggleSidebar);
        
        if (overlay) 
          overlay.addEventListener('click', toggleSidebar);
        
      });
    </script>
    {% include "python-runner.njk" %}
  </body>
</html>
